{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.invalid/schemas/mvp-report.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "status", "startedAt", "completedAt", "inputKind", "outputImages", "failedImages", "outsideTextMode", "engine", "sakuraFallbackRegions", "sakuraFallbackFailures"],
  "properties": {
    "schemaVersion": { "const": 4 },
    "status": { "enum": ["completed", "partial", "failed"] },
    "startedAt": { "type": "string", "minLength": 1 },
    "completedAt": { "type": "string", "minLength": 1 },
    "inputKind": { "enum": ["directory", "zip", "cbz", "image", "unknown"] },
    "outputImages": { "type": "integer", "minimum": 0 },
    "failedImages": { "type": "integer", "minimum": 0 },
    "outsideTextMode": { "enum": ["disabled", "text-free-opencv"] },
    "engine": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pipeline", "translator", "fallbackTranslator", "ocr"],
      "properties": {
        "pipeline": { "const": "MangaTranslator" },
        "translator": { "const": "Hy-MT2-7B-Q4_K_M" },
        "fallbackTranslator": { "const": "Sakura-GalTransl-7B-v3.7-IQ4_XS" },
        "ocr": { "const": "manga-ocr" }
      }
    },
    "sakuraFallbackRegions": { "type": "integer", "minimum": 0 },
    "sakuraFallbackFailures": { "type": "integer", "minimum": 0 },
    "cbz": { "const": "translated.cbz" },
    "errorCode": { "type": "string", "minLength": 1 }
  },
  "allOf": [
    {
      "if": { "properties": { "status": { "enum": ["completed", "partial"] } } },
      "then": { "required": ["cbz"] }
    }
  ]
}
